""" dictionary 연습문제 """
data=[
    {"id":1,"score":[80,90]},
    {"id":2,"score":[70,85]},
    {"id":3,"score":[95,60]}
]

result={}
for item in data:
    s_sum=0
    for s in item['score']:
        s_sum+=s
    
    if s_sum>=160:
        # dictionary 에 새로운 key와 값 한번에 넣기
        result[item['id']]='Pass'
    else:
        result[item['id']]='Fail'
# result:{1: 'Pass', 2: 'Fail', 3: 'Fail'}
print(f"result:{result}")
